[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

# Redirects for static export — all routes served from out/
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
